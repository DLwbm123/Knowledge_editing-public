# Stage24R2 数值修复验收

固定2^24反向尺度，2^20/2^24差异均低于0.1%；不改变损失系数或精度。确定性后端下OFF两次重复完全一致，ON有效更新非零。两例完整路径与prefix缓存的特征、损失、梯度及3/20步ON/OFF参数一致。

原R1失败结论保留。此验收只证明实现机械生效，效果待纯19自然路由对照。复用历史E0/E2权重；新后端没有重训完整对照，不能把差异全部归因HSIC。
