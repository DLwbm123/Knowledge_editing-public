# 小规模纯FACT / 原配方BalancEdit 对照

状态：19条候选及完整来源隔离CPU核查通过；用户已批准独立8小时/300项预算，并最新改为新服务器物理GPU0；待Base核验后训练。

固定比较只有两臂：A全部编辑逐条C_FACT+当前冻结HSIC Top1；B相同native和顺序逐条原配方BalancEdit。两臂同Base/runtime、精度、原生成和自然路由合同，保留原方法各自初始化、损失、步数与优化器。保持原有候选ID和ID派生seed；禁止新增共同NO_H背景、七配置、阈值调整、强制专家替代或中途换方法。

19条来自上轮训练前的STRUCTURAL_TASKS和资格记录，其中11个固定锚点加8个有H/U的额外native；顺序为11锚点后原候选顺序。旧混合STREAM曾按S轨清空后续H，不能用它恢复纯流支持。本次从未清空的原始结构冻结重建成员和H/U绑定，没有根据旧student分数选择候选。50不再是启动门槛，19仅称小规模纯流。

每条均满足作者参考已获AI来源审阅支持、native-only fit、非空合法H_fit/U_fit、全序列来源分区和上轮4090 Base原错。H为19个分配、15个不同QA/8图，U为19个分配、17个不同QA/17图；native19 QA/16图。最大H单QA复用3次，U复用2次。全序列native和辅助来源与6个冻结保护来源无组或图像身份交集，native与辅助图像也无交集；固定未训练改写与训练问句不同，但按定义共享native图像，不能宣称改写是独立图像评价。患者/研究仍UNKNOWN，AI支持不是医生签署。

硬件变化需先核验Base：同一19候选在已有A100 Base中18错、1对，在上轮4090中19错。先在指定GPU0对19 native和26核心输入作Base核验、按固定资格规则锁定全队列；不看新student结果。若该条仍正确，排除并报告纯18，11锚点保留；如锚点或其他项目资格出现变化，记录原因和实际固定面板合法性，不补数、不切混合流。不得假定跨卡输出逐token不变。

评价：自然路由下真实生成共同N=11及实际最终18/19端点的全部native与固定26核心输入（6 H、9 U、11未训练改写），逐次插入保存native输出、tokens、真实专家与权重绑定。固定锚点PairCorrect仍以11个锚点计权；同时报告H unique-QA正确性、保持/Fix及复用权重，U保持与完整固定U面板分数、未训练表达泛化。保留预先定义的U角色变化及明确分母，始终生成完整面板，禁止按student对错删输入。报告两臂实际激活专家、权重是否相同、文本/token差异、路由变化与逐条对错转移；不将所有专家切换自动视为错误。

扩到32/50/100需要逐条合法H_fit/U_fit，A仍全部FACT、B仍全部BE；不要求每条新增编辑都有专属H_eval。扩流不属于当前19候选的新资源申请，不能自动发起。

权重与成本：上轮新服务器的全部生成writer/BE/W0已按生命周期删除，当前兼容可复用writer为0。19条全部通过时需重建19条FACT初始化与continuation、19条BE；18条时相应为18+18。11个旧锚点也不能从已清理的文件恢复。旧16个FACT bank仍在my-gpu，属于旧DEV16固定层/旧来源绑定，不是本轮冻结HSIC纯流权重，禁止改名复用。预训练模型、环境、来源文件和评分证据可沿用；匹配输入、参考、输出文本及冻结Judge协议的判定可精确复用，不重判不满意的答案。

资源已获批，为新的独立预算，不继承上一轮余额：仅新服务器物理GPU0（RTX4090D）、累计GPU进程墙钟上限8小时（加载/资格/HSIC/失败/恢复/训练/生成全部计入），新增Judge判定上限300项。19+26个Base输入最多45项，N=11两臂37输入最多74项，最终19两臂45输入最多90项，主线保守合计209项；精确复用单列。留至少8GiB磁盘余量，完整19份BE约4.2GiB，另预留初始化、低秩、输出、临时保存空间；依据实际单原子任务耗费为共同端点留预算，不承诺时刻。资源不足时保存已完成共同纯流并如实收口。

每份权重保存到该实验全部登记的插入、前缀、最终生成和恢复消费者结束；保留原始输出、Judge材料、配置、router和运行血缘后，按已有生命周期清理。新实验不自动恢复旧混合流心跳，不申请其他GPU。
