# Stage19 FASTTRACK 终点相同的逐条核查

核查日期：2026-09-18。依据本地保存的 OUTPUTS.jsonl、STREAM、评分输入与原始 VERDICTS、训练验收和结果表。仅离线读取既有证据；没有新增训练、生成或语义判定。本补充与本次归档说明一同发布；原始身份映射仍仅私有保存。

## 直接证据

|核查项|N=11|N=32|
|---|---:|---:|
|每臂评价输入数|37|58|
|两臂选中同一逻辑专家的输入数|37|58|
|两臂实际权重绑定相同的输入数|0|34|
|两臂原始 token 序列完全相同的输入数|27|58|
|两臂语义正确性相同的输入数|31|58|

最终58个输入的文本和token在评分前已经相同。不能把得分相同归因于四舍五入。两臂共享Base路由key/radius和顺序，因此逻辑路由相同符合设计；锚点权重不同，共享背景权重相同。

## 最终实际激活的专家

- 11个锚点native：仍由各臂自己的锚点专家处理，权重不同，均答对。
- 21个背景native：由精确共享的背景专家处理。
- 6个H评价输入：全部由精确共享的背景专家处理。
- 9个U评价输入：7个由共享背景处理，2个仍由各臂锚点处理。
- 11个未训练native改写：仍由各臂锚点处理，权重不同，均答对。

N=11时有10个输入的两臂原始输出不同，其中6个语义正确性不同。到N=32，这10个输入全部切换到共享背景专家，最终权重与输出均相同。仍由锚点处理的24个固定输入，在各自臂内相较N=11输出均未改变。

## 结果解释

|指标|N=11 A|N=11 B|N=32 A|N=32 B|
|---|---:|---:|---:|---:|
|锚点PairCorrect|54.55%|27.27%|72.73%|72.73%|
|U保持|25.00%|0.00%|50.00%|50.00%|

终点相同说明本次共享背景压力测试中，原本区分两臂的输入后来由共同背景接管；不能据此断言两种锚点方法本身等效，也不能把N=11的小DEV差值宣称为稳健优势。最终H保持为2/4，U保持为4/8，两者都为50%但不是相同分母或相同样本。

## 评分及边界核查

从原有282项缓存开始，按冻结qualification、prefix11、final32输入和原始判定重建343项缓存，与实际最终缓存完全一致。评分键包含图像身份、问题、参考和原始回答；同输入同回答的跨方法复用符合既定协议。本次未发现缓存合并或分臂聚合错误。

固定H/U涉及的4个不同图像与全部训练native/H_fit/U_fit图像没有交集。训练验收记录显示11个FACT各执行320个H非零梯度步骤；共3520步。该核查基于保留的训练回执、输出与权重绑定；本轮权重已按生命周期清理，没有重新加载权重进行推理验证。
