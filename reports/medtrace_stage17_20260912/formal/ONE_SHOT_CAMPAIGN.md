# Finite Stage17 continuation — 2026-09-13

User requested a one-shot continuation of the remaining authorized experiment,
without per-method prompting. No cloud instance resize, purchase, new method,
new cohort, new clinical signoff or change to Astra/high is authorized here.

The existing C_NO_H single process is left untouched. A neutral detached parent
waits for its complete N=146 receipt and releases no active checkpoint. Its finite
GPU sequence is C_NO_H insertion replay, BE insertion replay, dependency cleanup,
LoRA-Perf-v1 single/sequential, GRACE single/sequential and BELoRA V2 effect-repaired
single/sequential. BE single is already complete and is not retrained. Other
baselines retain their native continuous update/collision semantics, not a bank
assembled from single states. New baseline initial adapter construction uses the
plan seed 20260912; per-edit seeds remain the frozen existing method namespaces.

Prefix 1/50/100/146 evaluates all preceding requests and their attached supported
probes. Every insertion also records its native output. The exact-input active
target map is generated only from frozen metadata before sequential output, used
only by evaluation, and never provided to the router. C/BE use full prefix Base
nearest routing then that entry's radius; only the selected immutable expert is
loaded on demand. Bank checkpoints do not duplicate full BE writer matrices.

Scope remains the frozen main T0 N=146 cohort and attached supported tasks. H/G
unsupported C_FACT/C_EXTRA branches and other task-specific support/clinical-review
gaps remain explicit; this is not a claim of complete ten-task or paper-exact
evaluation. No historical Stage15/16 experiment is repeated.

Each phase keeps its required resume state until generation and the state consumer
finish. Explicit generated-weight inventories are deleted after the last registered
consumer; raw output/token/binding/receipt files and pretrained weights are retained.
Cleanup checks project/mount/path and open-file safety and writes private receipts.
The 8GiB data-disk reserve and 22,000MiB next-worker admission margin remain enforced.
No concurrent GPU worker is launched. Input, state, nonfinite, storage or execution
failure stops the queue with evidence rather than changing scientific settings.

A separate finite local follower collects JSON/JSONL only, prepares the unchanged
isolated Astra/high packets, scores once, reports the main-cohort panels and pushes
only aggregate reports. Base and BE single accepted verdicts are reused; new
requests bind full realized input/state/generation and Judge context, not answer
strings. Transport or Judge failures do not restart training or accepted scores.
Publication failure is recorded as publication pending. The local follower requires
the Mac to stay awake and its SSH control connection and existing Codex login to
remain usable; remote GPU work does not depend on the Mac staying connected.

Checks before dispatch: 4 campaign tests (prefix visibility, finite order,
active-target timing, unsafe cleanup refusal and complete synthetic report paths)
and 5 existing Stage17 contract tests passed; both modules compile/import.
GPU phase checks execute on the authorized device after the existing worker exits.
This document records implementation, not completed experiment results.
