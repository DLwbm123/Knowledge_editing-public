# Stage24E-R2 结果分解与实际执行覆盖

- `run_id`: `stage24e-r2-20260920-job525`
- 固定公开基线提交：`4610eecef5b432bbbccee9483e2f69464a509150`
- 本次动作：只读现有输出、事件、判定、checkpoint 和账本；新增 GPU/Judge 均为 0；未重训、未重判、未启动 Stage25。

## 97 项的组成

每个臂的 97 项不是一个单一的 N19 面板，而是两部分：

1. 前缀 1–18 的 18 条 native 插入过程检查；
2. 前缀 19 的 79 条最终面板：19 native、22 H_eval、23 U_eval、11 native_text_extension、4 positive_image。

因此每臂运行输出为 `18 + 79 = 97`。此前发布的历史总数 `B0=82/97、B1=83/97、B2=83/97` 原样保留。对应的 82/83 包含前缀 1–18 过程检查和前缀 19 终点评价，不能把它解读成一个 97 条同质终点面板。

完整 N11 H/U/改写/正向面板没有在运行中产生；现有前缀 11 只有 1 条 native 插入检查，故 N11 分项指标保持 `null`，不填 0、不从 N19 反推。

## N19 分项结果

分数来自已有 291 条 R2-C Judge 结果；Base 正确性逐条精确复用冻结 `QUALIFIED_SCORE_CACHE`，没有新增 Judge 请求。H/U/text/positive 均只指前缀 19 面板；native 是终点 native，不等同于前缀 1–18 的过程检查。

| 臂 | native | H 总体 | H 保持 | H 修复 | U 总体 | U 严格保持 | 改写 | 正向 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| B0 | 19/19 | 13/22 | 9/22 | 4/22 | 18/23 | 18/23 | 10/11 | 4/4 |
| B1 | 19/19 | 16/22 | 10/22 | 6/22 | 17/23 | 17/23 | 11/11 | 2/4 |
| B2 | 19/19 | 14/22 | 8/22 | 6/22 | 19/23 | 19/23 | 11/11 | 2/4 |

来源等权结果（按匿名来源组先组内平均，再对来源组平均）：

| 臂 | H 来源等权 | H 来源组数 | U 来源等权 | U 来源组数 |
|---|---:|---:|---:|---:|
| B0 | 58.33% | 10 | 81.25% | 12 |
| B1 | 73.33% | 10 | 77.08% | 12 |
| B2 | 63.33% | 10 | 85.42% | 12 |

N19 终点面板评分覆盖为三臂均 `79/79`；整轮 R2-C 判定覆盖为 `291/291`。Base 缓存命中为 `291/291`。训练支持与未训练评价分开保存：native 属于 `native_or_support`，H/U/text/positive 属于 `untrained_panel`。

## B2 逐题配对变化

逐题记录见 `R2_PAIRWISE_TRANSITIONS.jsonl`，只公开匿名输入 ID、匿名来源组和布尔判定，不公开问题、图像、输出或 tokens。

| 比较 | 都对 | 都错 | 错→对 | 对→错 |
|---|---:|---:|---:|---:|
| B2 vs B1 | 63 | 12 | 2 | 2 |
| B2 vs B0 | 59 | 9 | 6 | 5 |

其中 native 及改写/正向仍按 panel role 单独计数；训练支持过程和未训练评价面板在逐题记录中以 `evaluation_partition` 明确区分。配对表不把 B2 的小幅变化解释成因果优势。

## 实际执行覆盖

事件流证据表见 `R2_EXECUTION_COVERAGE.json`。三臂均有 19 个 writer 的插入准备与生成证据；每臂前缀 1–18 有 18 条 native 过程输出，前缀 19 有 19 条终点 native 输出。

- B0：无维护事件，符合其无维护臂定义。
- B1：23 个维护槽均有 `MAINTENANCE_PREPARED`；实际只有 20 个槽产生 step，共 400 步。
- B2：23 个维护槽均有 `MAINTENANCE_PREPARED`；实际只有 21 个槽产生 step，共 420 步。
- 两臂双槽前缀均为 `t7,t9,t11,t12,t13`。但 `t5`、`t14` 在 B1/B2 都只有 prepared、没有 step，因此分别标为 `NOT_EXECUTED`，不是“已执行但无记录”。
- 事件流没有逐 writer W0 哈希/320 步优化器轨迹，故该项标 `NO_EVIDENCE`；不把配置文件声明当运行证据。
- `TEACHER_REPLAY.json` 有 36/36 条 `teacher_tokens_equal_current_Base=true`，但没有逐维护事件的 teacher 绑定记录；mask 状态在事件字段中不存在，标 `NO_EVIDENCE`。
- 事件流没有 Judge barrier 或“判定后才允许下一次写入”的事件，因此在线评分放行顺序标 `NO_EVIDENCE`；现存判定是生成后的 291 条批次评分。
- 完整 N11 终点评价标 `NOT_EXECUTED`；N19 前缀 19 的 79 条面板标 `EXECUTED_AND_SCORED`。

## Canary

R2-B 现存证据为 Judge `7/7` 正确。工程断言单列：R2-B `EXIT=0` 与 7 条 canary 输出有证据；save/load/event recovery、重复写入防护、未选专家和底座未修改、维护前后 consumer 绑定等没有现存逐项证据，均标 `NO_EVIDENCE`。此前 5 次工程失败保留在 GPU 账本中，不在本次补跑。

## 决策结论

**A（完整协议执行且分项可计算）不成立。** 原因是 B1/B2 各有两个 prepared 槽（t5、t14）没有实际 step，且在线 Judge barrier、逐 writer W0/320 步和逐维护 teacher/mask 证据缺失。历史 82/83/83 数字仍可复现并保留，但不应作为“完整维护协议”结果解释。

**B（协议有偏差，但部分结果仍可用）成立。** 可用部分包括：三臂实际生成的 18 条插入 native 过程检查、前缀 19 的 79 条终点评价、现有 291/291 Judge 覆盖、Base 的 291/291 精确缓存绑定，以及逐题配对变化。所有结果均应按已执行子集和上述覆盖限制引用。

**C（仍不可判断）** 包括完整 N11 面板、B1/B2 完整 23 槽维护的因果比较、责任归属/teacher-mask 对齐、在线评分屏障、save/load 恢复和未选专家/底座不变等工程断言，以及任何“B2 全面优于 B1/B0”的结论。没有新增实验授权，本次不补做这些缺口。

## 公开文件

本目录同时提供：分解结果 JSON、逐题匿名分数、B2 配对变化、执行覆盖矩阵、canary 证据覆盖、真实运行源码的脱敏副本及运行时哈希。原始医学数据、图像、输出、tokens、身份映射和权重均未公开。
