# Stage24E-R1：CPU计划已闭合，GPU保守预留未通过

**R1已推进到完整CPU维护计划、评分状态计数和资源核算；未启动GPU验收或三臂训练。** 状态为 `GPU_BOUND_EXCEEDS_CAP`。这不是责任对齐方法的负结果。上一版930消费者的初步报告保留；本次用结构状态计数替代它作为新判定的预算依据。

## 共同维护计划与目标政策

按已记录的真实前缀路由，只摄取截至该插入的原native/H/U支持，编译出每个回放臂23个维护槽：当前专家8槽、旧专家15槽，共460步。未处理事件保留原排队时间，winner改变时转交新责任者；最后仍有1项待处理，不追加清队列训练。随机和对齐臂使用完全相同的槽位。

按角色、不同QA数、来源数和目标token长度档（≤4、≤8、≤16、>16）匹配。H长度取既有实际训练目标token记录，U长度取不可变teacher token记录。随机子集允许抽到对齐样本；固定4096次合法匹配提议仍失败时回退同一合法子集，不能因此制造对照差异。32个角色槽有真实不同选择，覆盖17个插入时刻、18个差异来源，满足最低对照条件。逐步私有抽样表及来源到达账本已保存，公开槽位统计见 `ONLINE_SLOT_TABLE.json`。

原19的native/H/U参考均在已有冻结来源清单中为SUPPORTED。故U自首次到达时就已带有合法参考，不必等未来H出现；其中3个Base原错U从各自到达前缀起在三臂共同屏蔽U损失，保留原教师、不额外变成H加权。三个历史N45 H→U案例的时间测试保留，只是CPU政策测试，没有执行N45。

DEV79原表完整保留；新子面板排除精确训练输入及已知native改写谱系。DEV H为16QA/8来源、旧H为6QA/2来源。11道已知native改写仍在原改写指标中，但不算进“确未训练谱系”子面板。未声称检测到未知改写、患者独立或消除了历史DEV曝光。

## 评分预算：已从930消费者改为条件结构界

190个“前缀×已插入native”CPU原路由结果全部自归属，每题恰有一个零距离key。静态代码核查显示现有生成路径只挂载选中专家，残差仅依赖其A/B参数；不把整个bank或未选专家传入前向。该审计是原路径实现证据，不冒充尚未运行的新训练器验收。

| 类别 | 保守新判定预留 |
|---|---:|
| native符号状态：B0/B1/B2 = 19/42/42 | 103 |
| 11/19固定非native端点 | 360 |
| N19 H/U训练支持正确性 | 96 |
| 独立U教师一致性任务 | 51 |
| Base已完成精确缓存缺口 | 0 |
| 有限验收 | 20 |
| 失败及收口 | 40 |
| 合计 | **670/800** |

native仍需真实生成1052个消费者；并未删掉检查。每次写操作产生新的符号参数版本，未预支跨臂相同参数或历史命中率。额外固定端点/支持按不命中缓存预留。共享Judge仍为188/2000。

`JUDGE_BUDGET_CERTIFICATE.json`是**条件CPU证书**：依赖完整执行指纹及有限全路径GPU验收。已新增CPU指纹失效测试：输入、目标、专家版本、后端等依赖改变时失效；同一执行身份但tokens/送审文本/评分键变化必须抛出 `EXECUTION_IDENTITY_VIOLATION`。尚未实际验证的新worker、路由重提取、确定性、保存恢复均明确为NOT_RUN。

## 磁盘与GPU是独立门槛

磁盘实查可用9,131,450,368字节；三臂103个不可变低秩参数版本、活动恢复与原子副本、输出账本及临时文件预留211,238,912字节（约201.45MiB）。维持8GiB后仍余330,276,864字节。该存储设计不复制历史bank/teacher/W0，正式实施前仍须完成共享不可变工件哈希绑定；未删除或搬迁历史文件。

GPU用既有**同确定性后端**19个writer实测训练时间作为保守参考，并完整计入真实生成：

| 项目 | 秒 |
|---|---:|
| 三臂57个writer原始320步 | 7514.08 |
| 两回放臂维护 | 390.18 |
| 1052次native真实生成（历史最大native耗时×1.5） | 364.35 |
| 固定端点与支持真实生成（历史均值×1.5） | 291.29 |
| 有限验收、加载 | 600 |
| 失败及退出收口 | 300 |
| 合计 | **9459.91** |

超过阶段9000秒 **459.91秒（约7.7分钟）**。此外尚未加进GPU进程驻留等待逐次语义判定的耗时，因此目前不能保证全路径在阶段上限内收口。

这不是“实际必然超时”的数学下界：参考训练含HSIC，而本轮关闭HSIC，实际可能更快；也已检查历史Stage24R OFF短程计时，但它来自确定性修复前的profile，R2同后端OFF重复性报告没有对应分项耗时，未把两者拼成“同后端已测速”；部分U被屏蔽也可能降低耗时。但不能把未测的节省承诺为已有同后端证据，也不能把Judge复用当成省掉生成。共享GPU仍有10380.94秒，**共享余额不改变9000秒阶段上限**。

依照R1“CPU资源门槛通过后才做有限GPU验收”，本次停在CPU收口，没有擅自增加预算、降低检查、运行试训或启动N45/Stage25。后续若有可复用的同后端HSIC-OFF有效时长证据，可更新此保守估计；本报告没有把工程估算不足解释为科学失败。
