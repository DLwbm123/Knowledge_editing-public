# New episode source and role freeze

The complete authorized source screen found 16 new SLAKE train episodes on distinct source-image groups. This is not the earlier N=0 inspection of old DEV16. No student performance selected the cohort. Selection uses fixed dataset/image/source-ID order, one fact per existing-image-identity group, and the frozen V4 Base-before source verdict. Patient identity is unknown; different images are not proof of different patients.

The screened non-reserved pool contains 875 rows / 117 image groups: SLAKE 272/25 and VQA-RAD 603/92. Whole-pool identity clearance covers the current QUAL handoff, old heldout target/locality source supersets, all historical MedTRACE roles (including superseded freezes), and the complete 640-record LiveEdit identity ledger. No heldout/sealed answer file was opened; no additional source authority is required. Existing image hashes are reused for identity closure, not recalculated.

Source-only linguistic review was completed before new student outputs, by the executing agent, not a human or independent clinical reviewer. Native attributes are body region, specified-organ presence, MR weighting and largest visible organ. Four fit lexical questions and the approved V1 calibration context wrappers are retained as different role families. Evaluation separates two source-style interrogatives from two imperative attribute requests. These are source-style confirmation probes, not official M3Bench T2G or independent clinical annotations. Image generality is NA: no authorized equivalent image variants were found in this source-only pool.

H conflicts require the same explicitly reviewed clinical attribute and identical original annotated source question, with a distinct annotated value on another image. Different answers/images alone do not qualify. Same-question/same-answer contexts remain a separate challenge panel, not verified conflicts. U uses a different reviewed or source-annotated attribute on another image. Every lawful selected H is retained regardless of Base correctness; no success-based filtering. Source-image groups are isolated across roles within each independent edit; negatives may be shared across edits and are not independent samples.

All 16 episodes have fit H/U support. One has no H calibration/evaluation support; retain the episode and report NA for those cells. Actual support is in the private manifest and subsequently NEW_BASE_BEFORE_SUPPORT.csv. H_keep/U_keep are frozen using the same full-answer Judge execution on Base-before outputs, before any new BE/CP student starts. They are not chosen using student outputs.

Queue: old DEV16 BE first; then new scoped Base generation/Judge; then 16 blocks of BE, native-to-A2 initialization, P4 W0, P4 W1(.1), P4 W2 and L16 W2. Initializer failures remain in the cohort/queue denominator. All weights, raw questions/answers, input identities, Judge mappings and complete private manifests remain on the server.
